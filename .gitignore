venv/
__pycache__/
*.pyc

# Embedding cache: derived from corpus content, keyed by chunk
# hash, and rebuilt on demand. Committing it would put ~34 x 768
# floats in every diff for no benefit.
evaluation/.embed_cache.json
